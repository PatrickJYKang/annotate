# Brand

- `wordmark/` is the current identity: the lowercase "annotate" wordmark
  in IBM Plex Sans Bold and the lowercase "a" abbreviated mark, with
  favicon and social renders. Start there.
- `legacy-logo/` is the retired geometric A-with-arrow mark, its
  exploration rounds, and the earliest sketches under `concepts/`.
  Reference only.
