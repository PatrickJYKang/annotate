# annotate-sidecar dependencies
# Python 3.10+ required

# Web framework
fastapi>=0.104.0,<1.0
uvicorn[standard]>=0.24.0,<1.0

# Video / image processing
opencv-python>=4.8.0,<5.0
numpy>=1.24.0,<2.0

# Object detection & tracking (Phase 6)
ultralytics>=8.0.0,<9.0

# Segmentation (Phase 9)
# mobile-sam or segment-anything — install separately:
#   pip install git+https://github.com/ChaoningZhang/MobileSAM.git

# Homography estimation (Phase 8) — vendored from narya (MIT License)
# These are narya's runtime dependencies:
tensorflow>=2.10.0
torch>=1.12.0
torchvision>=0.13.0
kornia>=0.6.0
segmentation-models>=1.0.1
six>=1.15.0

# Scientific computing
scipy>=1.10.0,<2.0

# Export encoding (Phase 10)
# ffmpeg must be installed system-wide: brew install ffmpeg / apt install ffmpeg
